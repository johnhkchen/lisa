---
id: S-037-01
title: provider-aware-startup-grace-readiness
type: story
status: done
priority: critical
tickets: [T-037-01-01, T-037-01-02, T-037-01-03]
---

**Scope:** The bootstrap-readiness gating in crates/lisa-plugin/src/lib.rs — the SeatAssignmentState::Starting machine, acknowledge_process_start, and the assignment-delivery seam — plus the provider readiness capability at the AgentAdapter boundary in crates/lisa-plugin/src/adapter.rs. Owns the Codex grace-mode Starting→Delivering paced send without a synthetic readiness claim, keeps Claude's SessionStart-gated Starting→ReadyForAssignment path unchanged, and adds deterministic delayed-send and prompt-miss native tests. Consumes E-034's attempt lease and E-035's two-stage split; does not reopen them.

**Story acceptance:** A grace-mode (Codex) fresh launch, with no SessionStart emitted pre-prompt, remains Starting until a bounded named startup grace elapses, then attempts the tagged chat assignment and enters Delivering directly—never ReadyForAssignment or Owned from elapsed time. Only a matching current-attempt UserPromptSubmit publishes Owned. A SessionStart-mode (Claude) launch still reaches ReadyForAssignment via unchanged positive process-start evidence before Delivering. Injected-time native tests prove a delayed-send sequence to Owned-on-acceptance and a prompt miss that resolves in a bounded named state (retry then recycle/fail) without ever reaching Owned, with stale-attempt signals rejected and E-034 fencing intact.

**Honest boundary:** Everything here is deterministic, injected-time, and FREE — no provider tokens, no live Zellij PTY. The elapsed grace is a pacing envelope only and never publishes Owned. The single metered two-provider live rerun that actually closes the epic is S-037-02, named here as the deferred step, not hidden inside a ticket. This story does not alter Claude's SessionStart evidence path beyond keeping it intact, and does not make a truly unresponsive owned Codex process reusable.

## DAG

```
T-037-01-01  provider-readiness-capability  ← T-035-03-02
T-037-01-02  codex-startup-grace-pacing  ← T-037-01-01
T-037-01-03  delayed-send-and-prompt-miss-regression  ← T-037-01-02
```

Wave rationale: T-037-01-01 (the adapter provider-readiness capability) runs alone because both the grace transition and the tests depend on the settled readiness-mode shape, and it lives in adapter.rs, disjoint from the state machine. T-037-01-02 then lands the grace-pacing transition and T-037-01-03 adds the deterministic tests that assert its combined behavior; these two serialize because the tests exercise the exact transition 02 introduces and both edit the lib.rs Starting region.

**Out of this slice:** The live metered two-provider rerun (S-037-02); any change to E-034's single-writer fencing boundary; reusing a genuinely hard-silent owned Codex process; the real-Zellij stub regression already standing from S-035-02; and re-opening E-035's two-stage launch/assignment split.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
