---
id: S-039-02
title: typed-signal-ingestion-boundary
type: story
status: done
priority: high
tickets: [T-039-02-01, T-039-02-02, T-039-02-03]
---

**Scope:** The eight `check_*_signals` consumer loops in crates/lisa-plugin/src/lib.rs (heartbeat, process-start, shell-ready, acknowledgement, awaiting, transition, error, and the remaining poll) plus their shared pane-id/filename parsing; introduces a typed signal-ingestion boundary (typed signal records) over them while keeping provider payload/deletion/lease differences distinct.

**Story acceptance:** Characterization tests pinning current poll order, payload admission, legacy naming, delete timing, and per-consumer effect pass identically before and after; the eight loops read through one typed signal-ingestion boundary; regression tests lock the typed-record contract and prove poll order + attempt admission unchanged; cargo test --workspace and clippy green.

**Honest boundary:** Refactors ingestion SHAPE only — no signal's payload, deletion timing, or lease semantics changes (N1 — Accidental provider uniformity preserved) — and it does not touch downstream failure/timeout/publication transitions (sibling stories). Deterministic-fixture-proven; the live Codex-seat exercise is deferred to S-039-06.

## DAG

```
T-039-02-01  characterize-signal-consumers  ← T-039-01-01
T-039-02-02  introduce-typed-signal-ingestion-boundary  ← T-039-02-01
T-039-02-03  lock-ingestion-boundary-regression  ← T-039-02-02
```

Wave rationale: A strict bracketed sequence, not a fan-out: the characterization ticket (BEFORE) must land and pass before the structural edit, and the regression lock (AFTER) must follow the edit on the same seam — parallelizing would break the before/after guarantee. Runs after clippy for a green, attributable baseline.

**Out of this slice:** Failure/reclaim (C-06), timeout/liveness (C-07), and publication (C-08); a general iterator/callback scheduler framework beyond typed records; the C-12 scheduler test-fixture-writer consolidation.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
