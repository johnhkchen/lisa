---
id: S-045-01
title: assignment-claim-contract
type: story
status: open
priority: critical
tickets: [T-045-01-01, T-045-01-02]
---

**Scope:** The assignment-boundary data + command contract: an atomic assignment-file writer and the Lisa claim command surface, bound to E-034 lease fencing. Touches the CLI/plugin command surface and the assignment-file path adjacent to scheduler.rs; does not change Zellij injection, the ownership state machine, or the ticket boundary.

**Story acceptance:** Command-level tests show an assignment written atomically for a ticket+attempt+nonce, and a claim command that accepts the current attempt+nonce under a held lease while rejecting stale/prior-attempt or wrong-nonce claims with a named reason.

**Honest boundary:** Fixture-proven and free — no Codex tokens, no live Zellij. The writer and claim surface are defined and unit-tested here; wiring the claim into scheduler ownership transitions is deferred to the state-machine story, and the atomic-write step reuses any existing helper rather than inventing a general-purpose one.

## DAG

```
T-045-01-01  atomic-assignment-file-writer
T-045-01-02  claim-command-surface  ← T-045-01-01
```

Wave rationale: The writer runs first; the claim command depends on it because the claim references the attempt+nonce the writer stamps. Two tickets serialized — they share the one assignment/claim contract shape.

**Out of this slice:** The launcher argv, Zellij injection, the evidence-tiered ownership state machine, the clean-exit boundary, and any live field test — all owned by later stories. Does not alter E-034 lease semantics, only binds to them.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
