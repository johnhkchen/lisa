---
id: S-033-03
title: consecutive-reuse-field-evidence
type: story
status: open
priority: high
tickets: [T-033-03-01, T-033-03-02]
---

**Scope:** A live-style Codex reuse harness (building on the T-029-01 codex-runbook-live-run precedent) plus a committed deterministic reproduction of the original handoff stall; test/harness artifacts under `docs/active/work/` and the workspace's Codex regression tests. Reads the S-033-01 contract; does not modify scheduler logic.

**Story acceptance:** A committed deterministic test reproduces the fire-and-forget stall and now passes under the ack-gated contract, running in CI without a live Codex; and a harness run drives several back-to-back Codex reuses that each resolve to exactly one of the two named outcomes (ack-then-owned OR timeout-then-fallback) with zero silent stalls, while the same harness over a Claude seat shows unchanged behavior.

**Honest boundary:** The deterministic reproduction (T-033-03-01) is FREE and CI-runnable; the consecutive-reuse proof (T-033-03-02) is isolated as the live-style validation step. The harness capability starts from the pre-existing T-029-01 precedent rather than rebuilding unrelated Codex setup.

## DAG

```
T-033-03-01  deterministic-stall-reproduction  ← T-033-01-04
T-033-03-02  consecutive-reuse-live-proof  ← T-033-03-01
```

Wave rationale: T-033-03-01 lands first (free, deterministic, CI) and T-033-03-02 follows because it reuses the reproduction's harness scaffolding and is gated behind the free test being green — sequential by dependency and risk, not by file overlap.

**Out of this slice:** Building the live-run harness capability from scratch (T-029-01 is the precedent), the scheduler transition logic (S-033-01), and dashboard rendering (S-033-02).

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-11._
