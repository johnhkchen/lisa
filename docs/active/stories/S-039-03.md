---
id: S-039-03
title: named-failure-reclaim-transitions
type: story
status: open
priority: high
tickets: [T-039-03-01, T-039-03-02, T-039-03-03]
---

**Scope:** The scheduler failure/reclaim teardown paths in lib.rs — assignment delivery failure, assignment recovery failure, startup failure, startup recovery failure, error-signal handling, session timeout, and stale-thread reclamation — modeled as explicit named/typed transition outcomes without weakening scheduler authority (lease/seat/thread/pane/provenance/retry).

**Story acceptance:** A state-machine map + invariant test matrix pins current teardown under each authority and passes identically before/after; failure transitions become explicit named outcomes; regression tests prove recovery stays bounded and ends in a named actionable state (N2 — Infinite retries or silent babysitting); E-034 lease and E-035 recovery semantics are unchanged; gates green.

**Honest boundary:** Names/typifies transitions only — it does not merge the distinct authorities into one path (they genuinely differ by lease/seat/thread/pane/provenance/retry) and does not alter retry bounds or E-034/E-035 semantics. Fixture-proven; live-seat exercise deferred to S-039-06.

## DAG

```
T-039-03-01  map-failure-state-machine  ← T-039-02-03
T-039-03-02  name-failure-transition-outcomes  ← T-039-03-01
T-039-03-03  lock-recovery-named-state-regression  ← T-039-03-02
```

Wave rationale: Bracketed sequence: the state-machine map + invariant matrix (spike, BEFORE) must exist before transitions are modeled — the inventory names this as a hard prerequisite — the edit sits between, regression locks AFTER. Depends on C-05 because failure paths consume the just-typed signal records and both edit overlapping scheduler-poll regions of the same 14,943-line lib.rs; serialized to keep commits narrow (N4 — Broad rewrites for local defects).

**Out of this slice:** Timeout/liveness deadline unification (C-07) — session-timeout is characterized here but not merged into the deadline evaluator; publication (C-08); signal ingestion (C-05); weakening or generalizing provider retry semantics.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
