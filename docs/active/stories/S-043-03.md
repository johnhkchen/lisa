---
id: S-043-03
title: pane-time-attribution-quarantine-and-repro-guard
type: story
status: open
priority: high
tickets: [T-043-03-01, T-043-03-02, T-043-03-03]
---

**Scope:** The plugin attribution + surfacing path: crates/lisa-plugin/src/lib.rs read_usage (~4915) replaced by a CaptureRecord consumer, the teardown provenance fill (~lib.rs:4887), a session-id-keyed quarantine store, and operator-visible activity events; plus the field-incident repro encoded as a deterministic regression test.

**Story acceptance:** A recycled pane that runs ticket A then ticket B files A's summed tokens on A's provenance record and B's on B's — no record overwritten by the later recycle; a capture that owner_at cannot attribute lands in a session-id-keyed quarantine with a raised activity event instead of blending into a shared bucket; and the bug-report repro (six overwrites) run through the new path shows correct per-ticket attribution — failing against the old env-keyed logic and passing now.

**Honest boundary:** Attribution is proven against synthetic CaptureRecords and a deterministic repro (FREE); the live metered multi-pane loop re-verification the epic's 'done looks like' ultimately wants is authorized separately at the counter, named here, not folded into a ticket. Deferred and named: the codex ledger usage-join intermittent-null bug (distinct field finding from misattribution).

## DAG

```
T-043-03-01  attribute-captures-by-pane-time  ← T-043-01-01, T-043-01-02, T-043-02-01
T-043-03-02  quarantine-unattributable-usage  ← T-043-03-01
T-043-03-03  field-repro-regression-guard  ← T-043-03-01, T-043-03-02, T-043-02-02
```

Wave rationale: 03-01 (attribute by pane×time) is the spine and depends on both foundation tickets plus the writer; 03-02 (quarantine) extends the same consumer so it follows 03-01; 03-03 (repro guard) depends on both plus the loud no-write (02-02) because it asserts attribution AND surfacing together. A strict chain — each ticket edits the same consumer/teardown region, so no parallelism is safe within this story.

**Out of this slice:** Any new cost/usage reporting or dashboard view (N3 — this is record correctness, not a relabel); Claude/Codex cache-split parity; a broad scheduler rewrite (N4 — the existing ownership timeline is reused, not rebuilt).

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-13._
