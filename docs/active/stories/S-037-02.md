---
id: S-037-02
title: fresh-loop-live-provider-parity-rerun
type: story
status: open
priority: high
tickets: [T-037-02-01]
---

**Scope:** The metered validation slice: rerunning the S-035-03 installed-provider isolated startup harness against freshly built lisa binary+WASM for both a Codex-first and a Claude-first first-assignment, unattended, with no manual command or trust repair. Touches only the harness/runbook invocation and any small unattended-run wiring; no scheduler code.

**Story acceptance:** Both live controls pass in one unattended run: the Codex-first seat no longer hangs in Starting—after the bounded startup grace it attempts the tagged chat assignment and enters Delivering directly without a synthetic ReadyForAssignment claim, stays non-Owned, and publishes Owned only on a matching current-attempt UserPromptSubmit; the Claude-first seat reaches ReadyForAssignment via unchanged SessionStart evidence before Delivering and Owned. Neither requires manual command repair or trust intervention.

**Honest boundary:** This is the single metered, live-token validation the board authorizes; it runs against a freshly built binary+WASM in an isolated temporary project and must not hot-reload its parent loop. The trust-path canonicalization and the harness itself are the already-landed S-035-03 prerequisite, named here rather than rebuilt. It validates done-looks-like and adds no new scheduler behavior; a run reporting unmet acceptance is not auto-published Done.

## DAG

```
T-037-02-01  fresh-loop-live-provider-parity-rerun  ← T-037-01-03
```

Wave rationale: One ticket — the rerun is a single cohesive metered validation with no internal fan-out, so it does not parallelize; it runs last because it asserts the full post-fix contract against real providers.

**Out of this slice:** The scheduler grace contract and native tests (S-037-01); trust-model changes beyond the already-landed canonicalization; broad harness infrastructure; and making an unresponsive owned Codex reusable.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-12._
