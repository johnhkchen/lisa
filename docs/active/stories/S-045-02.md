---
id: S-045-02
title: per-ticket-launcher-argv
type: story
status: open
priority: high
tickets: [T-045-02-01, T-045-02-02]
---

**Scope:** The Lisa-owned launcher and the Zellij injection path: argv construction against the assignment file, and the change to inject only the launcher invocation per ticket. Touches the launcher binary/script and the injection call site; leaves the claim command and ownership state machine untouched.

**Story acceptance:** A fixture run shows the launcher spawning codex with the assignment path as a single non-interpolated argv element, and Zellij injecting only the launcher line (assignment body never appears in pane input) with a fresh TUI per ticket.

**Honest boundary:** Fixture/stub-pane proven and free; real Codex/Zellij spawning is exercised only in the field-test story. Assumes the assignment-file format from the contract story.

## DAG

```
T-045-02-01  launcher-argv-construction  ← T-045-01-01
T-045-02-02  zellij-injects-launcher-only  ← T-045-02-01
```

Wave rationale: argv construction runs first; the injection change depends on it since injection invokes the launcher. Serialized — both edit the launch path.

**Out of this slice:** How ownership is proven (claim/evidence tiers), the delivered-awaiting-claim state, ticket-boundary exit/revocation, and live field validation. Does not rewrite the Claude injection path — smallest contract only.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
