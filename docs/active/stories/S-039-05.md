---
id: S-039-05
title: atomic-publication-boundary
type: story
status: done
priority: high
tickets: [T-039-05-01, T-039-05-02, T-039-05-03]
---

**Scope:** The publication (atomic-rename) sites — fresh launch, assignment, lease marker, admitted artifact, and shell-readiness publication — together with artifact/ticket-completion/provenance emission, made an explicit atomic-publication boundary with typed options; per-site temp naming, serialization, filesystem side, collision behavior, and operator-facing errors preserved. Touches lib.rs publication paths and the provenance/commit-transaction seam.

**Story acceptance:** Characterization + hostile-path tests pin each site's temp naming, serialization, collision behavior, operator-facing error, and provenance integrity; an explicit atomic-publication boundary with typed options replaces the scattered renames with those contracts intact; regression + direct hostile-path tests lock the boundary; P1 — Preserve the repository (no residue, no cross-ticket mixing, no secret exposure) proven; gates green.

**Honest boundary:** Unifies the rename MECHANISM behind typed options only — the distinct security/attribution contracts per site are preserved, not flattened — and it does not change what gets published or when. Fixture-proven including hostile-path unit tests; live done-not-committed / staged-residue observations deferred to S-039-06.

## DAG

```
T-039-05-01  characterize-publication-sites  ← T-039-04-03
T-039-05-02  introduce-atomic-publication-boundary  ← T-039-05-01
T-039-05-03  lock-publication-hostile-path-regression  ← T-039-05-02
```

Wave rationale: Bracketed sequence (characterize+hostile-path → boundary edit → regression lock). Depends on C-07/C-06 because the launch/assignment publication sites overlap the lifecycle paths already restructured; last of the four structural stories so it publishes atop a settled scheduler shape, serialized on lib.rs for narrow commits.

**Out of this slice:** Signal ingestion (C-05), failure/reclaim (C-06), timeout/liveness (C-07); the deferred declarative-hook-schema (C-11) and historical-harness (C-10) items; changing provenance schema or commit-transaction contract semantics.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
