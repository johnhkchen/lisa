---
id: S-036-01
title: legible-cli-help
type: story
status: done
priority: high
tickets: [T-036-01-01, T-036-01-02, T-036-01-03]
---

**Scope:** crates/lisa-cli/src/main.rs only for behavior-free surface: the top-level #[command(about=...)] attribute, the Commands enum's per-variant /// doc comments and #[command(...)] grouping attributes; plus a new help-surface test under crates/lisa-cli/tests/. main()'s match dispatch, every subcommand's logic, and all flag definitions are untouched.

**Story acceptance:** `lisa --help` prints a plain about-line (no 'DAG-driven'/'orchestration'/category jargon), foregrounds the five operator commands (init, validate, status, doctor, loop), and visibly sets apart or hides the hook/plumbing commands (agent-exec, capture-usage, commit-ticket, complete-ticket) — while every one of the 12 subcommands still resolves and runs; each operator `lisa <cmd> --help` describes its action in plain English; a native test asserts the command set is unchanged and the operator-facing help carries no banned jargon.

**Honest boundary:** Copy + clap grouping only; entirely FREE (no tokens, no live agent run), verified by running the built binary's --help and a native `cargo test`. Deferred and NAMED: any clap subcommand nesting / structural redesign (the E-036 context flags this as a distinct follow-on, not hidden here), and any generated man page / README / long-form docs beyond the built-in --help.

## DAG

```
T-036-01-01  about-line-and-operator-internal-grouping
T-036-01-02  plain-verb-forward-command-help  ← T-036-01-01
T-036-01-03  lock-help-surface-regression-test  ← T-036-01-01, T-036-01-02
```

Wave rationale: The three tickets run mostly SERIAL rather than fanning out, because all copy and grouping edits land in the single Commands enum in main.rs and parallel edits there would collide. T-036-01-01 runs first — it settles the enum's grouping/ordering attributes and the about-line, fixing the structure everything else sits in. T-036-01-02 follows on that settled shape (pure copy inside the now-fixed grouping). T-036-01-03 runs last to lock the final rendered output; only its test file is disjoint from main.rs.

**Out of this slice:** No command or flag is renamed, removed, hidden-from-execution, or changed in dispatch behavior; main()'s match arms and every subcommand's logic stay as-is. No subcommand nesting or structural clap redesign, no man-page/README/shell-completion generation, and no changes to the four machinery-invoked commands beyond rewording their help text.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
