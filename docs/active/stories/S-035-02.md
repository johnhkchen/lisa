---
id: S-035-02
title: fresh-loop-startup-regression
type: story
status: done
priority: high
tickets: [T-035-02-01]
---

**Scope:** A deterministic, committed real-Zellij integration regression using a local stub provider, plus focused native state tests. It boots a fresh isolated layout and exercises both actual PTY boundaries: bounded bare-provider launch into the shell, then bounded attempt-tagged assignment injection into the live chat composer. It also deliberately leaves zsh at `dquote>` through truncated/missing-closing launch input and asserts process start, assignment acknowledgement, same-pane recovery, and seat state without spending provider tokens. Provider-neutral; owns the standing reproduction of T-034-03-02 and consumes S-035-01 and S-035-04.

**Story acceptance:** An automated real-Zellij test boots a fresh pane with a stub provider and proves process start reaches ReadyForAssignment, not Owned; a bounded chat reference to the complete attempt assignment reaches the composer; and only matching prompt acceptance reaches Owned. It fails on the pre-fix truncated/missing-closing-input behavior, then proves `dquote>` is interrupted, the same pane positively acknowledges shell readiness, and a strictly newer attempt repeats both stages successfully without consuming a spare. Fault modes suppressing reset, start, or assignment acknowledgement never reach Owned and resolve boundedly. Focused native tests retain E-033/E-034 invariants.

**Honest boundary:** The regression boots real Zellij but uses a deterministic local stub, so it is token-free and independent of Codex trust. Actual Codex/Claude validation remains S-035-03.

## DAG

```
T-035-02-01  deterministic-delivery-boundary-regression  ← T-035-01-02, T-035-01-04, T-035-04-02
```

Wave rationale: One ticket: the regression is a single cohesive standing reproduction with no internal fan-out, so it does not parallelize. It runs after S-035's delivery, generic recovery, and explicit `dquote>` same-pane recovery tickets land because it asserts their combined post-fix behavior.

**Out of this slice:** The installed Codex/Claude rerun and trust canonicalization (S-035-03); implementation (S-035-01); re-proving E-033/E-034 beyond regression assertions.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-12._
