---
id: S-035-02
title: fresh-loop-startup-regression
type: story
status: open
priority: high
tickets: [T-035-02-01]
---

**Scope:** A deterministic, committed real-Zellij integration regression using a local stub provider, plus focused native state tests. It boots a fresh isolated layout, exercises the actual plugin-to-shell PTY boundary, deliberately detects truncated/missing-closing launch input, and asserts process-start and seat state without spending provider tokens. Provider-neutral; owns the standing reproduction of T-034-03-02 and consumes S-035-01.

**Story acceptance:** An automated real-Zellij test boots a fresh pane with a stub provider, fails on the pre-fix truncated/missing-closing-input behavior, and after the fix proves the complete launch reaches the shell, the stub process actually starts, and Owned follows the matching start signal. A fault mode that suppresses start never reaches Owned and resolves boundedly. Focused native tests retain E-033/E-034 invariants.

**Honest boundary:** The regression boots real Zellij but uses a deterministic local stub, so it is token-free and independent of Codex trust. Actual Codex/Claude validation remains S-035-03.

## DAG

```
T-035-02-01  deterministic-delivery-boundary-regression  ← T-035-01-02, T-035-01-04
```

Wave rationale: One ticket: the regression is a single cohesive standing reproduction with no internal fan-out, so it does not parallelize. It runs after S-035's delivery and recovery tickets land because it asserts their combined post-fix behavior.

**Out of this slice:** The installed Codex/Claude rerun and trust canonicalization (S-035-03); implementation (S-035-01); re-proving E-033/E-034 beyond regression assertions.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-12._
