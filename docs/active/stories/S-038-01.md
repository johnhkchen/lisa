---
id: S-038-01
title: establish-release-baselines
type: story
status: open
priority: high
tickets: [T-038-01-01, T-038-01-02, T-038-01-03]
---

**Scope:** The measurement slice — deterministic size and startup/launch-timing observations plus caveated host-process memory observations of the freshly built RC (CLI binary + embedded WASM). Writes only to per-ticket docs/active/work/<id>/ artifacts; touches no source and no scheduler/adapter/provider contract.

**Story acceptance:** Three recorded baseline artifacts exist — CLI+WASM byte sizes, startup/launch timing (or an explicit not-deterministically-measurable note), and idle/active host-process footprint — each carrying the exact command that reproduces it, and the memory numbers are labeled host-process observations, not plugin-heap attribution.

**Honest boundary:** Captures the pre-tightening 'before' snapshot only; the 'after' numbers and the aggregated report belong to S-038-04. Memory is a host-process RSS observation, deliberately NOT an exact plugin-heap figure — that precise attribution is out of reach and is not claimed.

## DAG

```
T-038-01-01  cli-and-wasm-size-baseline  ← T-037-02-01
T-038-01-02  startup-launch-timing-baseline  ← T-037-02-01
T-038-01-03  caveated-memory-footprint-observations  ← T-037-02-01
```

Wave rationale: All three measurement tickets fan out in parallel: each writes to its own docs/active/work/<ticket-id>/ artifact, disjoint files with no shared mutation, and each is read-only against source. No ordering exists among size, timing, and footprint.

**Out of this slice:** Any source change to move a number (which would make before/after dishonest); the fmt/test/clippy gate (S-038-02); the cleanups (S-038-03); writing the report (S-038-04).

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
