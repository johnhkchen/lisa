---
id: S-033-02
title: handoff-state-legibility
type: story
status: open
priority: medium
tickets: [T-033-02-01]
---

**Scope:** `crates/lisa-plugin/src/ui.rs` dashboard rendering — render the recycled-Codex seat's `assigned-pending-ack`, `owned`, and `recovering` states as distinct at-a-glance indicators, driven by the scheduler state published in S-033-01.

**Story acceptance:** A dashboard snapshot/test shows a recycled Codex pane displaying a distinct `assigned-pending-ack` indicator while awaiting ack, flipping to `owned` on ack, and showing `recovering` after timeout — each label sourced from published scheduler state, never inferred from terminal contents (N3 — Dashboard-only correctness).

**Honest boundary:** Rendering only — this story reports the scheduler's state and never substitutes for it (N3 — Dashboard-only correctness); correctness of the transitions themselves is S-033's contract, verified there, not re-proven here.

## DAG

```
T-033-02-01  surface-handoff-state-in-dashboard  ← T-033-01-04
```

Wave rationale: A single ticket — the rendering slice is small and self-contained, so there is nothing to parallelize; it waits until S-033-01 has published the three states before the labels can be driven by them.

**Out of this slice:** The scheduler transitions themselves (S-033-01), general pane lifecycle names (E-032 owns those), and any observability beyond surfacing the three handoff states.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-11._
