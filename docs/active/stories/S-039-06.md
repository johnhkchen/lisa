---
id: S-039-06
title: rebuild-and-field-report
type: story
status: open
priority: high
tickets: [T-039-06-01, T-039-06-02]
---

**Scope:** Rebuild of the CLI and embedded WASM from the post-refactor tree (build.rs embedding), the full native + WASM fmt/clippy/test gate run, and the release field report separating deterministic proof from live Codex-seat execution observations.

**Story acceptance:** The CLI and embedded WASM are rebuilt from the tree with all four structural refactors + clippy landed; native and WASM fmt/clippy/test gates are green; a field report exists cleanly separating deterministic proof from live Codex-seat observations — accounting for assignment/reuse failures, retries, timeouts, stale panes, false delivery errors, staged/index residue, done-not-committed residue, and provenance integrity — with any behavior change or unexplained anomaly recorded and blocking Done.

**Honest boundary:** The entire epic is executed by authorized live Codex seats. This closing slice does not rerun the preceding work: it aggregates the observations and provenance produced by those seats, alongside deterministic fixture proof from the structural stories. It REPORTS observations; it does not fix new defects it surfaces (those become their own tickets/epics), and a discovered behavior change blocks Done rather than being patched here.

## DAG

```
T-039-06-01  rebuild-cli-and-embedded-wasm  ← T-039-05-03
T-039-06-02  live-codex-seat-field-report  ← T-039-06-01
```

Wave rationale: Two-ticket sequence: rebuild + green gate first, then the live field-report run consumes that exact build. Depends on all four structural stories (via the C-08 tail) and clippy so the report describes the finished pass, not a moving base.

**Out of this slice:** Any structural refactor (C-05..C-08 own those); fixing anomalies the report surfaces; the deferred inventory items C-09..C-14; rewriting historical admitted evidence (forbidden by the epic).

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-12._
