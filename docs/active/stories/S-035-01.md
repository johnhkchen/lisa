---
id: S-035-01
title: atomic-first-assignment-delivery-contract
type: story
status: open
priority: critical
tickets: [T-035-01-01, T-035-01-02, T-035-01-03, T-035-01-04]
---

**Scope:** The native-TUI fresh-loop first-launch path end to end in `crates/lisa-plugin/src/lib.rs` and its generated native SessionStart hooks: the dispatch arm that currently publishes fresh seats Owned immediately, the pane input-delivery seam, and provider-start signal production for both Claude and Codex. It owns the "publish Owned only after observed start" contract and replaces the fragile giant quoted launch line with a short shell-safe launcher/indirection or an equivalently acknowledged transport. The loop does not use headless `agent-exec`, so that path is not a valid Codex start producer here. E-033's recycled-Codex acknowledgement and E-034's attempt lease are consumed, not duplicated.

**Story acceptance:** A fresh first launch publishes Owned only after a provider-neutral native SessionStart/process-start signal is observed. Immediately after dispatch it is pending-start, never Owned; a matching start signal promotes it, while a missing signal reaches bounded named recovery/failure. The pane receives a bounded, shell-safe launch command that does not inline the full ticket prompt inside one long quoted terminal write; Enter is withheld until the complete launch transport is ready. E-033 acknowledgement and E-034 lease-fencing tests remain green.

**Honest boundary:** State and signal classification are fixture-proven and free. The real Zellij PTY regression is S-035-02 and the installed-provider rerun is S-035-03. This story does not reopen E-033/E-034 internals; it moves fresh ownership behind observed start and makes launch transport robust.

## DAG

```
T-035-01-01  process-start-signal-producer  ← T-034-03-02
T-035-01-02  atomic-first-launch-delivery  ← T-034-03-02
T-035-01-03  gate-owned-on-observed-start  ← T-035-01-01, T-035-01-02
T-035-01-04  bounded-startup-recovery  ← T-035-01-03
```

Wave rationale: T-035-01 (start-signal producer — agent_exec.rs + generated hooks) and T-035-02 (atomic first-launch delivery — lib.rs send_line_to_pane seam) run in parallel: disjoint files, and only the signal's name/shape must settle before the gate. T-035-03 (gate Owned on observed start) then T-035-04 (bounded startup recovery) serialize — both mutate the lib.rs dispatch/seat_assignments region, 04 extends the state machine 03 introduces, and 03 consumes 01's signal while landing after 02's same-file delivery change so the lib.rs edits stay ordered.

**Out of this slice:** E-033 recycled acknowledgement and E-034 lease fencing; the real-Zellij reproduction (S-035-02); trust canonicalization and installed-provider rerun (S-035-03); completion internals and general pane naming.

---
_Materialized by Vend's `decompose-epic` play — 4 ticket(s), 2026-07-12._
