---
id: S-039-01
title: clippy-hygiene-debt-retirement
type: story
status: done
priority: medium
tickets: [T-039-01-01]
---

**Scope:** The 13 pre-existing test-only all-target/all-feature Clippy findings across the workspace test code (lisa-core / lisa-plugin / lisa-cli test modules and crates/lisa-cli/tests/), under both the native and wasm32-wasip1 clippy invocations. No non-test product source is touched.

**Story acceptance:** `cargo clippy --workspace --all-targets --all-features` and the wasm-target clippy both emit zero warnings; the 13 previously-known findings are gone with a recorded before(13)/after(0) count and reproducing command; cargo fmt and cargo test --workspace stay green; no non-test product source changed.

**Honest boundary:** Scope is exactly the 13 named findings; it does not chase new findings later introduced by the structural stories (each re-verifies its own gate) and does not touch product-source semantics to satisfy a lint. The work is mechanical and has no live-provider behavior dependency, but its assigned Codex seat remains part of the authorized end-to-end field observation.

## DAG

```
T-039-01-01  clear-test-only-clippy-debt  ← T-038-04-02
```

Wave rationale: One ticket — the debt is a single bounded, mechanical unit with one gate; splitting it would add ceremony without a real seam. It runs first so every downstream structural story starts from a green clippy baseline and any new finding is attributable to that story.

**Out of this slice:** The C-05..C-08 structural refactors; the deferred inventory items C-09 and C-11..C-14; reformatting beyond what lint resolution requires; changing product-source behavior to silence a lint.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-12._
