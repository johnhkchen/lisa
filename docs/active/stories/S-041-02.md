---
id: S-041-02
title: generated-invariant-evidence
type: story
status: done
priority: high
tickets: [T-041-02-01, T-041-02-02, T-041-02-03]
---

**Scope:** The proof layer for the completion contract, living entirely in lisa-core test modules and Cargo dev-dependencies: a deterministic regression replaying the recorded Arcade Midsummer T-009-01-01 event sequence through the aggregate, proptest / proptest-state-machine (dev-only) generating permuted event orderings, and the workspace + wasm32-wasip1 green-gate. No production source changes — the S-041-01 module contract is consumed read-only, not modified.

**Story acceptance:** The deterministic test feeds the recorded T-009-01-01 sequence (review.md present before phase→review, stop, ~10-min timeout, reload, confirming manual result) and asserts exactly one completion Request and exactly one authoritative Confirmed — never a re-injected finish-up over the existing artifact; the generated proptest holds the four invariants across permuted orderings (an admitted passing Review is never stranded, a blocked Review never completes, at most one live completion effect, at most one authoritative Done); and `cargo test --workspace` passes while lisa-plugin still builds to wasm32-wasip1 within the existing size budget, with proptest excluded from the wasm artifact.

**Honest boundary:** Deterministic and generated tests only — no live provider, no metered Codex seat, zero tokens; this story proves the domain contract in isolation, not the end-to-end field path. Reproducing the livelock against a live scheduler with the plugin adapter wired in is the follow-on Arcade-harness epic (e), named there, not attempted here. The recorded sequence is a fixture transcription of the field signal, not a re-run of the incident.

## DAG

```
T-041-02-01  recorded-livelock-regression  ← T-041-01-03
T-041-02-02  generated-invariant-proptest  ← T-041-01-03
T-041-02-03  workspace-wasm-green-gate  ← T-041-02-01, T-041-02-02
```

Wave rationale: T-041-02-01 (deterministic recorded-sequence repro) and T-041-02-02 (proptest deps + generated invariants) fan out in parallel — both consume the settled S-041-01 contract read-only, touch disjoint test modules, and share no edit. T-041-02-03 (green gate) is a barrier: it must consume the finished tree with both test bodies present and the dev-deps added, then prove workspace tests and the wasm size budget together on a settled base — mirroring E-040's rebuild-gate barrier.

**Out of this slice:** Any change to the S-041-01 reducer/eligibility contract (a discovered defect blocks Done and spawns new work, it is not patched here); the plugin adapter, journal persistence, `[d]one` authority, and live Arcade harness (follow-on epics b–e); enriching or rewriting historical provenance/admitted evidence; and adding new provider handshake semantics beyond provider-neutral consumption (N1 — Accidental provider uniformity).

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
