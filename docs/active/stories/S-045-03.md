---
id: S-045-03
title: evidence-tiered-claim-ownership-state-machine
type: story
status: open
priority: critical
tickets: [T-045-03-01, T-045-03-02, T-045-03-03]
---

**Scope:** The scheduler ownership state machine in scheduler.rs: claim-as-ownership-proof, the evidence tiers (matching hook = supplemental fast evidence, valid current-attempt private artifact = bounded fallback), and the delivered-awaiting-claim state with no re-injection. Owns the assignment/liveness ownership transitions; does not touch launcher argv or the boundary exit.

**Story acceptance:** Scheduler tests show ownership transitioning on a claim alone, a matching hook accelerating a pending claim and a current-attempt artifact serving as bounded fallback while stale-attempt evidence is ignored, and a slow first response entering delivered-awaiting-claim with zero duplicate injection and a named actionable resolution on timeout.

**Honest boundary:** Fixture-proven and free — driven by stubbed claims/hooks/artifacts, not live Codex. The live proof that a real slow Codex turn yields delivered-awaiting-claim is deferred to the field-test story.

## DAG

```
T-045-03-01  claim-is-ownership-proof  ← T-045-01-02
T-045-03-02  evidence-tiers-hook-and-artifact  ← T-045-03-01
T-045-03-03  delivered-awaiting-claim-no-reinject  ← T-045-03-02
```

Wave rationale: Three tickets serialized because they all edit the same ownership path: claim-as-proof first, then evidence tiers on top, then the awaiting-claim state that depends on both. No safe parallelism across the shared state machine.

**Out of this slice:** Launcher/injection (prior story), ticket-boundary exit and attempt revocation (next story), and dashboard labels — which report but never substitute for these scheduler transitions.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-13._
