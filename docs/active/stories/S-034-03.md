---
id: S-034-03
title: split-brain-regression-and-live-proof
type: story
status: open
priority: high
tickets: [T-034-03-01, T-034-03-02]
---

**Scope:** A deterministic regression alongside the existing provider-contract harness plus scheduler test scaffolding — a fixture reproducing the T-031-02 timeline (slow/silent old attempt → timeout → replacement whose prompt injection misses → old pane resumes) — and a fresh-loop live proof. Owns the standing reproduction; consumes S-034-01 and S-034-02 and changes no scheduler logic.

**Story acceptance:** A committed deterministic test drives the full split-brain timeline and asserts fence-before-reschedule, rejection of every resumed old-pane signal, no duplicate ownership, no cross-pane artifact attribution, and exactly one provenance record; and a live fresh-loop proof shows the same boundary holding with Claude's assignment/completion behavior unchanged across the identical harness (P3 — Provider parity at the contract boundary/N1 — Accidental provider uniformity).

**Honest boundary:** The deterministic reproduction is fixture-proven and free. The live proof runs in an isolated temporary project using a freshly built/installed lisa binary and WASM, because the parent loop cannot hot-reload scheduler code produced by earlier tickets.

## DAG

```
T-034-03-01  deterministic-split-brain-regression  ← T-034-02-01, T-034-02-02, T-034-02-03, T-034-02-04
T-034-03-02  live-proof-and-claude-parity  ← T-034-03-01
```

Wave rationale: T-034-03-01 must land before T-034-03-02 because the live proof runs the committed harness. Both follow the complete lease and rejection mechanism.

**Out of this slice:** The lease mechanism itself (S-034-01/S-034-02); general observability/pane naming (E-032); any new recovery path beyond E-033's bounded fallback; changing completion-commit internals beyond lease gating.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-11._
