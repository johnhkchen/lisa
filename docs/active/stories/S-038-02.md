---
id: S-038-02
title: clean-fmt-test-check-clippy-gate
type: story
status: open
priority: high
tickets: [T-038-02-01, T-038-02-02, T-038-02-03]
---

**Scope:** The workspace quality gate across lisa-core/lisa-plugin/lisa-cli: cargo fmt, clippy (native + wasm target), cargo test --workspace, and the WASM check. May edit source only as fmt/clippy require; changes no public CLI surface, scheduler, or provider-contract behavior.

**Story acceptance:** cargo fmt --all -- --check is clean, clippy emits zero warnings on native and the wasm target, and cargo test --workspace plus the WASM build/check pass — with the passing commands and output recorded.

**Honest boundary:** Formatting- and lint-only edits; no behavior change and no dependency churn (N4 — Broad rewrites for local defects/N2 — Infinite retries or silent babysitting honored). Runs after S-038-01 locks the before-numbers so gate edits cannot pollute the baseline. Landing demonstrated-value cleanups is S-038-03, not here.

## DAG

```
T-038-02-01  cargo-fmt-clean  ← T-038-01-01, T-038-01-02, T-038-01-03
T-038-02-02  clippy-zero-warnings  ← T-038-02-01
T-038-02-03  confirm-tests-and-wasm-check-green  ← T-038-02-02
```

Wave rationale: Strictly sequential: fmt must precede clippy (clippy edits would otherwise be reformatted), and the green-confirmation runs last against the final tree. All three touch or verify the same source tree, so parallelizing them would race.

**Out of this slice:** Refactors beyond what a lint demands (that is S-038-03); changing test expectations to force green; touching E-034/E-035/E-037 behavior or the clap CLI surface.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
