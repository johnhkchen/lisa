---
id: S-042-04
title: hostile-order-regression-and-field-gate
type: story
status: done
priority: high
tickets: [T-042-04-01, T-042-04-02, T-042-04-03]
---

**Scope:** The regression-and-field-evidence layer: a deterministic integration test that drives the REAL lisa-plugin adapter (not just lisa-core) through the recorded Arcade hostile sequence in the same two-level nested-monorepo topology, plus plugin-restart-reconstruction and lost-result/duplicate-stop fixtures; then a rebuild of the CLI + embedded WASM and one isolated disposable live Codex-seat field run bound to that build. Consumes the finished A/B/C surface read-only; adds tests and a field report, no new production contract.

**Story acceptance:** Driven through the real plugin adapter over the recorded sequence in a Lisa project two levels below its Git root (private review present before/through Implement→Review, stop during a slot transition, timeout/reload, delayed-or-duplicated command result, attempted `[d]one`), the effect uses Git-root-relative `games/midsummer/docs/...`; a PASSING Review yields exactly one completion commit, one authoritative Done row, seat release, and dependent scheduling, and a BLOCKED Review yields none; restart-reconstruction and lost-result fixtures converge on the prior commit rather than a second; no false finish-up prompt appears while review exists. A rebuilt CLI+embedded WASM then drives one disposable live Codex seat through normal artifact completion AND `[d]one` recovery in that nested topology, recording argv/correlations/journal/provenance/commit-tree, tearing down the fixture, and marking any unexplained behavior BLOCKING; `cargo test --workspace` passes and release WASM stays within the size budget.

**Honest boundary:** The deterministic regression and reconstruction fixtures are free and token-free; the single live Codex-seat field run is the one metered, human-authorized step in this epic — disposable, isolated, torn down, and explicitly bound to the freshly rebuilt CLI+WASM. It exercises normal completion and `[d]one` recovery only; it is field evidence, not an exhaustive live matrix, and unexplained behavior blocks rather than being patched inside the report.

## DAG

```
T-042-04-01  hostile-order-real-adapter-regression  ← T-042-01-07, T-042-02-03, T-042-03-03
T-042-04-02  restart-reconstruction-lost-result-fixtures  ← T-042-04-01
T-042-04-03  live-codex-seat-field-run  ← T-042-04-01, T-042-04-02
```

Wave rationale: T-042-04-01 (deterministic hostile-order regression against the real adapter) is the barrier consuming all of A, B, and C landed — passing and blocked outcomes both. T-042-04-02 (restart-reconstruction + lost-result/duplicate-stop fixtures) extends that harness and depends on it. T-042-04-03 (rebuild + live seat) runs last and alone: it needs a green deterministic tree and a fresh build before spending a metered seat, so it cannot precede the fixtures.

**Out of this slice:** Any new production completion contract or fix (a defect the regression surfaces blocks Done and spawns new work, it is not patched here); broad scheduler rewrites (N4 — Broad rewrites for local defects); a macro-generated FSM or subscriber-only tracing island; a live matrix beyond the one disposable seat; and any change to E-041's reducer or E-040's disposition gate.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-12._
