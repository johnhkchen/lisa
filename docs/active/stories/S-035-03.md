---
id: S-035-03
title: codex-first-trust-free-rerun
type: story
status: open
priority: medium
tickets: [T-035-03-01, T-035-03-02]
---

**Scope:** The enabling plumbing for a trust-free, isolated fresh-loop startup rerun on macOS — canonicalizing the harness fixture temp-dir path (resolving the /var -> /private/var symlink) before the Codex project-trust pregrant so Codex's resolved cwd matches the trusted entry — plus extending the existing isolated harness/runbook (docs/knowledge/codex-day-runbook.md lineage, the S-034-03 harness) to drive a freshly built lisa binary+WASM through a Codex-first and a Claude-first first-assignment. Touches the harness/runbook and the Codex trust-pregrant fixture setup; no scheduler code.

**Story acceptance:** The harness fixture's pregranted Codex trust path equals Codex's canonicalized resolved cwd (asserted by a check with no live run), and the isolated startup harness drives a freshly built lisa through both a Codex-first and a Claude-first first-assignment with no manual command repair and no manual trust intervention. The single live metered execution confirming Owned-only-after-start for both providers is the deferred validation of done-looks-like.

**Honest boundary:** Canonicalization is fixture-proven and free. The final ticket runs the installed-provider harness in an isolated temporary project using the freshly built CLI/WASM; this board explicitly authorizes that validation. It must not rely on hot-reloading its parent loop.

## DAG

```
T-035-03-01  codex-trust-path-canonicalization  ← T-034-03-02
T-035-03-02  fresh-loop-live-startup-harness  ← T-035-02-01, T-035-03-01
```

Wave rationale: T-035-03-01 is independent trust-pregrant plumbing and can run early. T-035-03-02 depends on it, the real-Zellij regression, and the landed fix because it asserts the same Owned-only-after-start behavior against real providers.

**Out of this slice:** The scheduler fix (S-035-01), provider-neutral Zellij regression (S-035-02), trust-model changes beyond fixture canonicalization, and broad harness infrastructure.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-12._
