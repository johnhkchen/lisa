---
id: S-040-03
title: hostile-path-regression-and-field-report
type: story
status: done
priority: high
tickets: [T-040-03-01, T-040-03-02, T-040-03-03, T-040-03-04]
---

**Scope:** The proof layer: deterministic hostile-path/restart regressions in lisa-plugin/src/lib.rs tests (and any fixtures) that pin the two lifecycle boundaries the T-039-06-02 and rc.6 incidents exposed; a rebuild of the CLI and embedded WASM from the post-change tree (build.rs embedding) with native+WASM fmt/clippy/test green; and a single authorized live Codex-seat field report separating deterministic proof from live observation.

**Story acceptance:** One regression fails on the pre-fix scheduler and passes now, proving a blocking Review keeps the ticket assigned with Done never prepared and dependents blocked; a second proves a pre-ownership miss leaves a durable failure row retrievable through the CLI surface; the workspace rebuilds with all gates green on both targets; and a field report exists that cleanly separates the deterministic fixture proof from the live metered Codex-seat observations, recording any behavior change or unexplained anomaly as blocking Done rather than patching it here.

**Honest boundary:** The two regressions and the gate run are deterministic and require no live provider-behavior harness. The closing field report launches an isolated, disposable LIVE and METERED harness using the exact CLI/WASM rebuilt by T-040-03-03; execution of the closing ticket by the already-loaded outer loop is not itself proof of the new scheduler. This story REPORTS live observations and blocks Done on anomalies; it does not fix defects the report surfaces (those become their own tickets/epics).

## DAG

```
T-040-03-01  review-block-hostile-regression  ← T-040-01-03
T-040-03-02  pre-ownership-miss-hostile-regression  ← T-040-02-02, T-040-02-03
T-040-03-03  rc-rebuild-and-green-gate  ← T-040-03-01, T-040-03-02
T-040-03-04  authorized-codex-field-report  ← T-040-03-03
```

Wave rationale: T-040-03-01 (review-block regression) and T-040-03-02 (pre-ownership regression) fan out in parallel: they pin disjoint boundaries whose features already landed in S-040-01 and S-040-02, and touch separate test cases. T-040-03-03 (RC rebuild + green gate) is a barrier — it must consume the finished tree with both regressions present, not a moving base. T-040-03-04 (authorized live field report) runs last and alone, against that exact build.

**Out of this slice:** Any change to the S-040-01 gate or S-040-02 ledger contracts (those stories own them; a discovered defect blocks Done and spawns new work, it is not patched here); token/cost enrichment and stale-marker cleanup; rewriting historical admitted evidence (forbidden by the epic); adding new provider handshake semantics beyond what rc.7 already ships (N1 — Accidental provider uniformity).

---
_Materialized by Vend's `decompose-epic` play — 4 ticket(s), 2026-07-12._
