---
id: S-045-04
title: clean-exit-attempt-revocation-boundary
type: story
status: open
priority: high
tickets: [T-045-04-01, T-045-04-02]
---

**Scope:** The ticket-boundary lifecycle in scheduler.rs: clean TUI exit, attempt/nonce revocation, fresh-TUI relaunch, and the exactly-one-authoritative-completion guarantee. Touches completion and boundary transitions; relies on the launcher and state machine already in place.

**Story acceptance:** A ticket driven through claim→work→boundary exits its TUI, revokes the prior attempt (a late claim on it is rejected), launches a fresh TUI for the next ticket, and records exactly one completion — with Claude-path and E-034 lease tests still green.

**Honest boundary:** Fixture-proven and free; the live boundary across a real ticket-to-ticket Codex handoff is proven in the field-test story. Does not change Claude's boundary behavior or lease semantics.

## DAG

```
T-045-04-01  clean-exit-revoke-attempt  ← T-045-02-02, T-045-03-03
T-045-04-02  one-authoritative-completion  ← T-045-04-01
```

Wave rationale: Clean exit + revocation runs first; the single-completion guarantee depends on that boundary existing. Serialized — both edit the completion/boundary path.

**Out of this slice:** Building the launcher or state machine (prior stories) and the live field test (next story). Explicitly leaves the Claude path and E-034 lease fencing unchanged — Claude need not share Codex's handshake.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
