---
id: S-045-05
title: real-codex-real-zellij-field-test
type: story
status: open
priority: high
tickets: [T-045-05-01, T-045-05-02]
---

**Scope:** The real-Codex + real-Zellij field-test harness (hooks disabled) that starts Lisa into existing Review tickets, plus the regression assertions encoding the epic's Done. Touches test/harness scaffolding and captured-signal fixtures; changes no scheduler contract.

**Story acceptance:** With installed Codex and Zellij and hooks off, the harness reproduces the 2026-07-13 false-failure on the old path and shows the new path delivering: fresh TUI per ticket, claim-first ownership, slow response → delivered-awaiting-claim with no duplicate injection, stale claim rejected, clean boundary with one completion — any unexplained behavior flagged blocking.

**Honest boundary:** This is the LIVE, metered step — real Codex tokens and real Zellij, authorized by the human at the counter, named here rather than hidden in a ticket. Everything upstream was fixture-proven; this story converts the incident into durable regression evidence and is the only place stub-acknowledgement is explicitly disallowed as closing evidence.

## DAG

```
T-045-05-01  real-codex-zellij-field-harness  ← T-045-02-02, T-045-03-03, T-045-04-02
T-045-05-02  field-regression-assertions  ← T-045-05-01
```

Wave rationale: Harness first, then the assertions that run inside it — serialized, since the assertions consume the harness's captured signals. Runs only after all contract stories land, because it exercises them end-to-end.

**Out of this slice:** Any new scheduler behavior (owned by the contract stories); it observes and asserts, it does not implement. Does not close on stub-acknowledgement tests.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-13._
