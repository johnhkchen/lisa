Advance P1, P2, P3, and P5 while respecting N1, N2, and N4: close the Codex timeout/retry split-brain exposed by T-031-02. When a Codex execution attempt becomes silent and times out, lisa currently releases its logical slot without terminating or fencing the old process, then may schedule the same ticket on another pane; if prompt injection into that replacement pane misses, the replacement is recorded as assigned while the old pane can resume, write the ticket's shared artifacts, and appear to complete work under the replacement pane's scheduler/provenance identity. Build an attempt-scoped single-writer lease for ticket execution: revoke and fence the old attempt before rescheduling, bind acknowledgements and lifecycle signals to the current ticket attempt, reject stale prior-attempt activity, prevent old artifacts from advancing or completing the replacement attempt, and publish exactly one authoritative outcome/provenance record. Preserve E-033's acknowledged Codex handoff and bounded fresh-session fallback instead of duplicating them; preserve Claude behavior. Add a deterministic T-031-02-shaped regression (slow old pane, timeout, replacement prompt miss, old pane resumes) plus a fresh-loop live proof showing no duplicate ownership, no cross-pane artifact attribution, and bounded actionable recovery.
