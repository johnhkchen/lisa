Advance P1, P2, P4, and P5 while respecting N2 and N4: fix the Review completion livelock observed in Arcade Midsummer T-009-01-01. A current Codex attempt wrote a complete private `.lisa/attempts/T-009-01-01/1/work/review.md` and stopped, but Lisa left the ticket in `phase: review`; roughly ten minutes later it injected the generic Review-timeout finish-up prompt even though the artifact already existed. The agent verified and updated the same review, stopped again, and the ticket still did not complete. The dashboard's `[d]one` operator recovery also failed to close it. Only a relaunched agent directly invoking `lisa complete-ticket` produced the completion commit. Make Review completion level-triggered and idempotent rather than dependent on one poll/stop edge: an admitted current-lease review must either enter a visible pending completion transaction or expose a durable named rejection; repeated polls, stopped signals, timeouts, pane relaunches, and `[d]one` must converge on that same transaction without duplicate commits. Add an Arcade-shaped regression covering review present before/through the Review transition, stopped and timeout paths, and manual `[d]one` recovery; prove one completion commit, one authoritative Done outcome, seat release, and dependent scheduling. Preserve attempt fencing, exact-path isolated commits, and the structured Review pass/block gate introduced by E-040; this signal concerns reliable delivery into that gate, not bypassing it.
