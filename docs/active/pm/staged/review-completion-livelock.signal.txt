Advance P1, P2, P4, and P5 while respecting N2 and N4: fix the Review completion livelock observed in Arcade Midsummer T-009-01-01. A current Codex attempt wrote a complete private `.lisa/attempts/T-009-01-01/1/work/review.md` and stopped, but Lisa left the ticket in `phase: review`; roughly ten minutes later it injected the generic Review-timeout finish-up prompt even though the artifact already existed. The agent verified and updated the same review, stopped again, and the ticket still did not complete. The dashboard's `[d]one` operator recovery also failed to close it. Only a relaunched agent directly invoking `lisa complete-ticket` produced the completion commit.

Replace the scattered edge-triggered completion calls with one provider-neutral, typed completion aggregate and reducer. Encode legal lifecycle states as a Rust enum carrying only state-valid data (for example Eligible, Requested, CommandInFlight, Rejected{reason,retryability}, Confirmed) and encode poll/artifact/disposition/stop/timeout/reload/manual-command-result inputs as typed events. The reducer returns an explicit transition outcome plus effect commands; it never launches commands or mutates unrelated scheduler state internally. Eliminate boolean completion results: already-pending, stale-lease, disposition-blocked, dependency-blocked, launch-failed, and accepted are distinct exhaustive variants rendered in the dashboard/activity ledger. Treat `[d]one` as explicit operator authority with an auditable source, not as an accidental reuse of the active attempt's authority.

Make completion level-triggered and restart-reconcilable. On every poll and plugin load, derive eligibility from durable current-lease artifact admission plus E-040's explicit pass disposition and compare it with the aggregate: an eligible ticket without a pending/confirmed transaction must request one; a command-in-flight state must have a correlation ID and bounded reconciliation; a lost in-memory edge cannot erase the obligation. Persist the minimal completion intent/result journal atomically (or reconstruct it unambiguously from durable ticket, artifact, command-result, and provenance state), and give `complete-ticket` an idempotency key such as ticket+attempt+completion generation so replay converges on the existing completion commit instead of duplicating it.

Separate the pure domain module into lisa-core from the Zellij/WASM effect adapter in lisa-plugin. Use Rust's enums, exhaustive match checking, newtypes for attempt/completion identities, `Result<Transition, CompletionRejection>`, and ownership to prevent illegal combinations at compile time. Consider `thiserror` only for typed error display/source plumbing; keep the state machine handwritten and inspectable rather than introducing a macro-generated FSM. Add `proptest` or `proptest-state-machine` as a dev-only candidate to generate event orderings (review-before-phase, stop-before-poll, duplicate results, reload, timeout, manual recovery) and assert invariants: an admitted passing Review is never stranded, a blocked Review never completes, at most one completion effect is live, and at most one authoritative Done outcome exists. Evaluate `tracing` only if it can feed Lisa's existing durable/operator-visible activity model under WASM; do not add a subscriber-only diagnostic island.

Add an Arcade-shaped deterministic regression covering review present before/through the Review transition, stopped and timeout paths, plugin-state reconstruction, command launch/result loss, and manual `[d]one` recovery; then add generated transition-sequence tests. Prove one completion commit, one authoritative Done outcome, seat release, and dependent scheduling. Preserve attempt fencing, exact-path isolated commits, bounded retries, small WASM size, and the structured Review pass/block gate introduced by E-040; this epic concerns reliable delivery into that gate and transaction, not bypassing either.
