Advance P1, P2, P4, and P5 while respecting N1, N2, and N4: integrate E-041's typed completion reducer into the real scheduler and close the Arcade T-009-01-01 livelock end to end. This is one convergence epic with four explicit stories and a final live barrier, not another pure-domain slice.

Story A — effect adapter and level-triggered reconciliation. Replace every scattered production `request_completion` entry (artifact/poll, stopped, idle, timeout/reload reconciliation, externally observed Done, and manual UI) with typed events into the E-041 reducer. The plugin adapter alone executes returned effects. On every poll and plugin load, derive durable eligibility from current attempt admission plus E-040's pass/block disposition; an eligible passing Review with neither pending nor confirmed state must converge into the transaction. Remove or quarantine the old boolean path so tests prove there is one production completion gateway. Render every named rejection and correlation identity in the activity/dashboard state.

Story B — durable intent, result, and idempotency. Persist the minimal completion aggregate or an equivalent append-only intent/result journal using Lisa's existing atomic publication patterns. Add a completion-generation/idempotency newtype bound to ticket+attempt; pass it to `lisa complete-ticket`. Replaying a request after plugin reload, lost command result, duplicate stop, or timeout must discover/converge on the prior completion commit, not create a second commit. CommandInFlight receives a bounded reconciliation deadline and ends in a named retryable/action-required state. Preserve exact-path isolated commits, attempt fencing, and backward-compatible provenance.

Story C — operator recovery as a real command. `[d]one` always emits explicit `OperatorRequested` authority independent of whether an active thread exists; it does not silently borrow the attempt lease. The reducer still enforces E-040's structured block/pass decision and dependencies, so operator authority cannot bypass a blocked Review. The modal remains open or provides durable visible confirmation until the request is accepted/rejected; already-pending and every rejection show the named reason and correlation. Add tests for active Review, orphaned Review, blocked disposition, stale attempt, already-pending, launch failure, and successful operator recovery.

Story D — hostile-order regression and field gate. Drive the real plugin adapter (not only lisa-core) through the recorded Arcade sequence: private review exists before/through Implement→Review; stop arrives during an inconvenient slot transition; timeout/reload occurs; command result is delayed or duplicated; operator `[d]one` is attempted. Assert a passing Review yields exactly one effect, completion commit, authoritative Done row, seat release, and dependent scheduling; a blocked Review yields none. Add plugin restart reconstruction and lost-result fixtures. Rebuild CLI+embedded WASM, then run one isolated disposable live Codex-seat harness explicitly bound to that build. The field report must exercise normal artifact completion and `[d]one` recovery, record correlations/journal/provenance/commit tree, tear down the fixture, and mark unexplained behavior BLOCKING rather than patching it inside the report.

Crate discipline: prefer standard Rust enums/newtypes/exhaustive matches and the E-041 reducer. `thiserror` may format typed errors. Keep proptest/state-machine tooling dev-only. Do not adopt a macro FSM or a subscriber-only `tracing` layer. If structured tracing is evaluated, it must adapt into Lisa's existing durable event/activity representation and remain optional; correctness cannot depend on a subscriber. Measure release WASM and reject a dependency that materially expands it without demonstrated value.
