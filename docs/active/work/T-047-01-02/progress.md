# Progress — T-047-01-02 probe rematch on RC surface

## Current status

- Research: complete.
- Design: complete.
- Structure: complete.
- Plan: complete.
- Implement: complete for all work possible without the human-operated run.
- Review: pending at the time of this entry.
- Expected Review disposition: block.

## Ownership baseline

The shared worktree was dirty before this attempt.

Pre-existing changes included:

- Lisa-managed `.lisa/completion-journal.jsonl`;
- Lisa-managed `.lisa/provenance.jsonl`;
- a concurrent modification to `crates/lisa-plugin/src/lib.rs`;
- active ticket frontmatter changes;
- shared work artifacts for other tickets; and
- other concurrent ticket state.

This attempt did not stage, reset, stash, overwrite, or commit those paths.

The attempt-private work directory initially contained only:

- `.lisa-launch-2.sh`; and
- `assignment-1-1784246933646337000.md`.

## Phase artifact execution

Created `research.md`.

It records:

- ticket and assignment boundaries;
- the published landing-probe rubric;
- the two 2026-07-16 baselines;
- the intended one-axis comparison;
- prerequisite purpose and summary code;
- observed version identifiers;
- evidence inventory;
- worktree ownership; and
- acceptance-state facts.

Created `design.md`.

It evaluates:

- authoring a surrogate page;
- scoring source strings as page output;
- relabeling a historical page;
- inferring the result from prerequisite tests;
- launching the metered run autonomously; and
- evidence-gated publication with a named block.

The chosen design is evidence-gated publication.

Created `structure.md`.

It defines:

- private attempt artifacts;
- possible operator-supplied run inputs;
- the eventual dated HTML and README row unit;
- an optional miss-ticket unit;
- the evidence-admission boundary;
- scoring and version boundaries;
- exact isolated commit shape; and
- ordering constraints.

Created `plan.md`.

It sequences:

- ownership setup;
- baseline mapping;
- candidate verification;
- evidence search;
- the evidence gate;
- evidence validation;
- all four rubric scores;
- pass/miss branching;
- documentation publication;
- validation;
- isolated commit checks;
- progress; and
- Review.

The four pre-Implement phase artifacts contain 1,191 lines in total:

- `research.md`: 206;
- `design.md`: 307;
- `structure.md`: 304; and
- `plan.md`: 374.

All were written to the attempt-private directory.

Lisa detected the artifacts and materialized the shared work directory as part
of its publication workflow.

This attempt did not directly write phase artifacts to
`docs/active/work/T-047-01-02/`.

## Prerequisite verification

Used `git merge-base --is-ancestor` to verify that the current branch contains:

- `e43f4d5` — T-046-07-01 CLI purpose copy;
- `e90ae07` — T-046-07-02 README purpose copy;
- `6633cf4` — T-046-07-02 generated context orientation;
- `4d1384e` — T-047-01-01 managed-session purpose context;
- `e150f40` — T-047-01-01 factual run outcomes; and
- `c4675cb` — T-047-01-01 single-source workflow copy.

Every ancestry check passed.

The dependency completion commit is:

`528ca498c720f869774685bf13fd64235941437b`.

The canonical context source says Lisa runs coding agents like Claude Code and
Codex through the ticket board so the operator does not approve every step by
hand.

The run-summary source contains:

- completed ticket counts;
- zero-approval output guarded by real tracking;
- latest-run failure and timeout handling;
- latest-run baseline offsets; and
- existence-checked ledger/work evidence paths.

No prerequisite implementation gap was discovered that this measurement ticket
could fix without the field run.

## Version verification

The root workspace manifest reports:

`0.4.0-rc.8`.

The installed executable path is:

`/Users/johnchen/.local/bin/lisa`.

The installed executable reports:

`lisa 0.4.0-rc.8`.

The ticket calls the intended train `0.4.1-rc`.

No inspected Cargo manifest currently reports a `0.4.1` version.

Therefore a future run record must capture the actual executable version and
revision rather than copying the ticket's train label into the series row.

This distinction is an input to the manual run, not grounds to fabricate a
different version.

## Evidence-gate execution

Listed `docs/knowledge/landing-probes/`.

It contains exactly:

- `2026-07-16-a-direct-codex-mini.html`;
- `2026-07-16-b-loop-built-claude.html`; and
- `README.md`.

No third or newly dated page is present.

Listed the attempt-private directory after the planning phases.

It contains Lisa inputs and the four phase artifacts.

It does not contain:

- a generated probe HTML file;
- a probe run record;
- an exact nested model identity;
- a nested agent CLI version;
- a probe fixture/container identity;
- a prompt record;
- a hands-off/intervention record; or
- a post-run summary capture.

Searched the benchmark and attempt directory for T-047-01-02 and rematch
evidence.

The matches are instructions and phase-artifact discussion only.

No independently operated run output was found.

The current T-047-01-02 provenance entry belongs to this assignment execution.

It is not the requested landing-page agent run and was not used as a proxy.

Evidence gate result: closed.

## Implementation result

No surrogate `lisa-tour.html` was generated.

No 2026-07-16 baseline artifact was copied or relabeled.

No rubric value was inferred from source strings.

No new row was appended to the public series.

No model, method, fixture, or surface identifier was guessed.

No external metered agent CLI was launched on John's behalf.

No copy ticket was created because there is no observed page miss to describe.

No Rust or documentation source file was changed by this attempt.

No `lisa commit-ticket` command was run because no admissible shared source
unit exists.

No ordinary `git add`, `git add -A`, or `git commit` command was used.

## Acceptance status

### Criterion 1

Not met.

There is no new dated landing-probe artifact.

There is no new series-table row.

The required method, model, exact surface, and scores cannot be recorded from
the available evidence.

### Criterion 2

Not met and not scorable.

There is no new page on which to evaluate Actors, Benefit, or Evidence.

The prerequisite source makes all three concepts available to a managed agent,
but source readiness is not the comprehension measurement.

No rubric reinterpretation was made.

No concrete copy ticket is warranted until an authentic page exposes a miss.

## Verification performed

- Confirmed all six prerequisite source commits are ancestors of `HEAD`.
- Confirmed workspace version `0.4.0-rc.8`.
- Confirmed installed Lisa version `0.4.0-rc.8`.
- Confirmed the public series has two HTML artifacts and one README only.
- Confirmed the attempt has no manual probe inputs.
- Confirmed no landing-probe shared path is staged in the ordinary index.
- Confirmed the attempt artifacts are ignored through `.lisa/.gitignore`.
- Confirmed no ticket-owned shared source change requires an isolated commit.

Cargo tests were not rerun.

This attempt changed no Rust code, and the relevant prerequisite commit already
publishes its full workspace and focused test results.

The missing verification is intentionally the manual, metered field run.

## Plan deviations

The evidence-absent Branch A executed exactly as planned.

Steps 6 through 14 were not applicable because they require a generated page
and run metadata.

No unplanned shared file mutation or commit occurred.

## Remaining operator action

John must run the loop-built landing probe on the intended RC surface.

The run should hold entry b's method constant where practical.

Before starting, choose and record whether the candidate is the current
`0.4.0-rc.8` executable/revision or a later executable that actually identifies
as `0.4.1-rc`.

Place in this attempt's work directory:

- the unedited generated HTML;
- run date;
- exact model;
- agent CLI name and version;
- exact Lisa version and source/release revision;
- fixture/container identity;
- prompt identity;
- hands-off/intervention outcome; and
- captured post-run summary.

Once present, the page can be scored and the public documentation unit can be
committed with exact paths.
