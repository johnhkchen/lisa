---
id: T-043-01-02
story: S-043-01
title: pane-time-ownership-lookup
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: []
---

## Context

Expose the scheduler's durable per-attempt (pane_id, started_at, ended_at, ticket_id) intervals as an owner_at(pane, captured_at) lookup, so attribution can key on pane×time instead of inherited env.

_Advances: P2 — State means what it says; P5 — Field evidence becomes regression evidence_

## Acceptance Criteria

- [ ] A plugin unit test over a pane recycled A(t0..t1)→B(t2..t3): owner_at(pane, t∈A-window)=A, owner_at(pane, t∈B-window)=B, owner_at(pane, t outside both)=None.
