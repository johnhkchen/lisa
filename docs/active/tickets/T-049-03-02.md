---
id: T-049-03-02
story: S-049-03
title: enforce-mode-and-no-git-leg
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-049-03-01, T-049-01-02]
---

## Context

The honesty rules that keep the ladder trustworthy, plus the field instrument. Enforce mode: explicit `[guards] completion = "commit"` hard-fails at preflight when unsatisfiable — the fintech stance; it never downgrades. No silent switches: a run that resolved commit-sealed and later cannot commit parks via S-049-04's machinery instead of quietly journal-sealing — asserted at the completion boundary. And the Chromebook-test runbook (docs/knowledge/chromebook-install-test.md) gains a **no-git fixture leg**: `/cbt/prepare` in a bare folder, agent runs to completion journal-sealed, doctor's "journal-only" line quoted in the record — making "a bare folder finishes its tickets" a scored sentence in the standing protocol. Executing the leg stays manual and metered per the Chromebook-test discipline; this ticket ships the runbook diff and the fixture flag, not the run.

_Advances: the ladder cannot lie — enforcement is hard, downgrades are loud, and the bare-folder claim joins the protocol that keeps every other install claim honest._

## Acceptance Criteria

- [ ] Enforce-mode tests: explicit commit + missing identity → hard preflight failure with remedy; auto-resolved commit + mid-run git breakage → parked completion with ask, zero journal-sealed rows written for that ticket.
- [ ] The runbook gains the no-git leg (fixture flag, pass criteria including the doctor journal-only line and a hash-verifiable journal row) consistent with the scripted `/cbt` ritual.
