---
id: T-033-01-04
story: S-033-01
title: bounded-wait-and-fresh-session-fallback
type: task
status: open
priority: critical
agent: codex
phase: done
depends_on: [T-033-01-03]
---

## Context

Bound the ack wait so timeout moves the seat to the named `recovering` state and spawns a safe fresh Codex session for the same ticket.

_Advances: P3 — Provider parity at the contract boundary; P2 — State means what it says_

## Acceptance Criteria

- [ ] Scheduler test — with ack withheld, after a finite configurable deadline the seat transitions `assigned-pending-ack` -> `recovering` and performs at most one fresh Codex launch for the same ticket with the un-acked assignment abandoned cleanly; fallback failure ends in a named actionable error (no double ownership, lost ticket, infinite retry, or silent stall).
