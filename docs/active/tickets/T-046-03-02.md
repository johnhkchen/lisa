---
id: T-046-03-02
story: S-046-03
title: stable-channel-repair
type: task
status: done
priority: high
agent: codex
phase: done
depends_on: [T-046-03-01]
---

## Context

`releases/latest` resolves to v0.3.0 (2026-06-21) because every tag since is a v0.4.0-rc.* prerelease, while the brew tap (publish-prereleases=true) serves rc.8 — so the README-recommended one-liner installs a month-old Lisa whose Codex leg predates the E-045 claim-path fix. Prepare and verify everything needed for a stable v0.4.0 cut from the current line: the release checklist, the auto-release/dispatch path, and the post-cut verification. Publishing the release is John's decision and action — this ticket delivers the checklist and the verification, and stops there.

_Advances: the blessed install path serves the Lisa we actually stand behind; channel skew stops handing Codex users the known-broken configuration._

## Acceptance Criteria

- [ ] A release checklist exists covering the cut, and the pipeline path (tag → release.yml → assets incl. musl artifacts) is verified end-to-end short of publishing.
- [ ] After John cuts the release: `releases/latest` resolves to a version containing the E-045 claim path, the README one-liner installs it (recorded), and shell-installer vs brew-tap skew is either eliminated or documented as deliberate.
