---
id: T-046-06-02
story: S-046-06
title: baseline-run-record
type: task
status: done
priority: critical
agent: codex
phase: done
depends_on: [T-046-06-01]
---

## Context

Record the pre-fix world before it disappears: John manually runs the baseline protocol against **today's** README on the fixture — expected to fail along the incident's chain (v0.3.0 via releases/latest skew, ~/.cargo/bin PATH stumble, cargo-first Zellij hint, compile spiral). This failing record is the preserved field evidence the epic closes against, and every string the agent quotes back is a string T-046-04-02 must fix. The runs are human-operated and metered (real agent tokens): this ticket's agent-side work is preparing, verifying, and recording — if the manual run evidence is not present in the work artifacts, the honest outcome is a named blocked state, never a fabricated result.

_Advances: a real failure is not closed by a plausible patch alone — the reproduction is captured first, so the fix can be proven against it._

## Acceptance Criteria

- [ ] At least one baseline run per leg (claude+Haiku-class, codex+mini-class) is recorded in the runbook's template, including wall time, disk delta, artifacts left behind, and verbatim quotes of the Lisa/docs strings the agent followed.
- [ ] Findings are filed as tickets or linked to existing E-046 tickets; anything the fixed stories don't already cover is surfaced, not absorbed silently.
