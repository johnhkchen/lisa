---
id: T-041-01-03
story: S-041-01
title: level-triggered-eligibility
type: task
status: done
priority: critical
agent: codex
phase: done
depends_on: [T-041-01-02]
---

## Context

Derive completion obligation level-triggered from durable inputs so a lost in-memory edge cannot strand an eligible ticket, and bound command-in-flight reconciliation to a named actionable state via correlation id.

_Advances: P2 — State means what it says; P3 — Provider parity at the contract boundary_

## Acceptance Criteria

- [ ] A pure `reconcile(durable_inputs, state)` returns a request effect when current-lease artifact admission plus an explicit pass disposition make a ticket eligible and no pending/confirmed transaction exists, returns none once one does, and maps an unresolved command-in-flight to a bounded, correlation-tagged actionable outcome rather than an unbounded retry; unit tests cover eligible / ineligible / already-pending / in-flight-reconcile cases, and no branch is Claude- or Codex-specific.
