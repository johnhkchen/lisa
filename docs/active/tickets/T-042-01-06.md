---
id: T-042-01-06
story: S-042-01
title: nested-monorepo-path-regression
type: bug
status: open
priority: critical
agent: codex
phase: ready
depends_on: [T-042-01-05]
---

## Context

Turn the simultaneous Arcade T-009-02-01/T-009-03-01 Review stalls into a deterministic command-and-transaction regression using the same two-level nested project topology.

_Advances: P5 — Field evidence becomes regression evidence; P1 — Preserve the repository_

## Acceptance Criteria

- [ ] A temporary Git repository containing a Lisa project at `games/midsummer` drives the real completion command builder and `complete-ticket` transaction: the pre-fix argv (`--path games/midsummer` plus `docs/...`) fails the fixture assertion, while the fixed argv targets the repository root, commits only `games/midsummer/docs/active/tickets/<id>.md` and its work directory, leaves root-level `docs/...` untouched, and returns one commit id.
