---
id: T-049-01-02
story: S-049-01
title: seal-visibility-and-ledger-field
type: task
status: done
priority: critical
agent: codex
phase: done
depends_on: [T-049-01-01]
---

## Context

Never a silent tier. Surface the resolved seal everywhere state is read: `lisa doctor` reports it in plain words ("completion seal: commit-sealed — finished work lands as history" / "journal-only — finished work is recorded but not undoable"); `lisa status` carries the same line; and every provenance record and completion-journal row gains an additive `seal` field so an auditor can never mistake journal-sealed work for commit-sealed work. Schema changes are additive: existing rows without the field parse unchanged, and replay/reconstruction paths treat absence as commit-sealed history from before the ladder existed (true by construction).

_Advances: the tier in effect is operable at a glance, and the audit trail says which seal bound each completion — states mean what they say, in the ledger and on the screen._

## Acceptance Criteria

- [ ] Doctor and status fixture tests assert the plain-language seal line for both tiers; copy follows the brand voice and never says "git" on the journal-tier line.
- [ ] Ledger and journal rows carry `seal` on new writes; parse tests prove old rows without the field load unchanged and are classified as pre-ladder commit-sealed.
