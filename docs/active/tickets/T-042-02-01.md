---
id: T-042-02-01
story: S-042-02
title: completion-generation-idempotency-key
type: task
status: open
priority: critical
agent: codex
phase: ready
depends_on: [T-042-01-06]
---

## Context

Add a completion-generation/idempotency newtype bound to ticket+attempt and make `lisa complete-ticket` idempotent — discovering the prior commit for a repeated key instead of writing a second.

_Advances: P1 — Preserve the repository; durable-at-most-one-commit_

## Acceptance Criteria

- [ ] CompleteTicketRequest carries a ticket+attempt+completion-generation key, and a test invoking `complete-ticket` twice with the same key produces exactly one commit (the second discovers and returns the prior commit id) while a different key is unaffected.
