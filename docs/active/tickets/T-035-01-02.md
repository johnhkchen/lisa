---
id: T-035-01-02
story: S-035-01
title: atomic-first-launch-delivery
type: task
status: open
priority: critical
agent: codex
phase: ready
depends_on: [T-034-03-02]
---

## Context

Replace the fragile long inline first-launch command with a bounded shell-safe launcher/indirection, or an equivalently acknowledged delivery primitive, so the full ticket prompt is not embedded in a giant double-quoted PTY write that can stop at `dquote>`.

_Advances: P2 — State means what it says; P3 — Provider parity at the contract boundary; E-035_

## Acceptance Criteria

- [ ] Fresh launch sends a bounded shell-safe command whose size does not grow with ticket prompt length; the full payload is prepared outside the PTY write and safely addressed by the launcher. Enter is never queued for a partial transport. Unit tests cover long/control/quote-heavy prompts, and the real-Zellij ticket proves actual delivery.
