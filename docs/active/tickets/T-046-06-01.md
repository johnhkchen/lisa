---
id: T-046-06-01
story: S-046-06
title: fixture-and-runbook-hardening
type: task
status: done
priority: critical
agent: codex
phase: done
depends_on: []
---

## Context

Materialize the Chromebook-test fixture and harden the runbook against first contact. Build docker/chromebook-test/Dockerfile per docs/knowledge/chromebook-install-test.md: Debian bookworm (Crostini's current default), with curl, ca-certificates, sudo, node, and the claude + codex CLIs — and deliberately without git, Rust, xz-utils, or build tools, because their absence is part of the test. Verify both agent CLIs launch and authenticate inside the container per the runbook's auth-hygiene notes (fresh auth, never mounted host config). Whatever the first spin-up teaches — package-set corrections, node-version reality, auth wrinkles — gets folded back into the runbook so it is followable by someone without this repo's authorship context.

_Advances: the field incident becomes a deterministic, repeatable fixture — evidence infrastructure, not anecdote._

## Acceptance Criteria

- [ ] The fixture builds; both agent CLIs launch and authenticate inside it following only the runbook; resource caps and snapshot steps work as written.
- [ ] Every deviation discovered during spin-up is reflected in the runbook (or the runbook's open-items list where deferred, e.g. real-device package-set diff).
