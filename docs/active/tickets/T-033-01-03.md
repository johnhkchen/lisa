---
id: T-033-01-03
story: S-033-01
title: gate-ownership-on-ack
type: task
status: open
priority: critical
agent: codex
phase: done
depends_on: [T-033-01-01, T-033-01-02]
---

## Context

Record `owned` only when the acknowledgment detector fires, so the scheduler never marks a recycled seat owned before Codex accepts.

_Advances: P3 — Provider parity at the contract boundary; P2 — State means what it says_

## Acceptance Criteria

- [ ] Scheduler test — a recycled seat with no ack never transitions to `owned`; injecting the matching ticket-scoped ack transitions `assigned-pending-ack` -> `owned` exactly once, while a stale or duplicate ack cannot claim the seat.
