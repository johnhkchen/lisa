---
id: T-046-06-03
story: S-046-06
title: closing-acceptance-run
type: task
status: open
priority: critical
agent: codex
phase: review
depends_on: [T-046-06-02, T-046-01-02, T-046-02-02, T-046-03-02, T-046-04-02, T-046-07-01, T-046-07-02]
---

## Context

The epic's gate. With the floor enforcement, managed runtime, static artifacts, and string fixes landed, John manually reruns the protocol: a low-end agent handed the README install section must produce a doctor-green Lisa hands-off. Pass requires the primary matrix (both legs) to hit every positive criterion — `lisa doctor` exit 0, `lisa loop --dry-run` clean in a scaffolded project, ≤10 minutes, disk delta within the runbook's calibrated bound (300 MiB with recorded composition — recalibrated 2026-07-16 from the real legs, commit b303ccc) — **and** every negative assertion: no rustc/cargo/rustup present afterward, no compiler invoked, sudo/apt actions recorded and boring. The seeded-failure variant (Zellij 0.40.1 on PATH) must record the hazard's outcome honestly: with the platform-aware managed default the old binary is never consulted, so a recorded pass showing the hazard designed out of reachability satisfies the variant; Lisa's error strings remain the fallback contract where the hazard is still reachable (explicitly pinned system mode). Human-operated and metered, same evidence discipline as the baseline ticket: absent run evidence means a named blocked state, not an assumed pass.

_Amended 2026-07-17 by the operator, taking the reviewer's own offered remedy ("explicitly amend both acceptance requirements"): disk bound aligned to the runbook's evidence-backed recalibration; variant criterion updated to accept designed-out-of-reachability. See operator-note-2026-07-17.md in the work directory._

_Advances: the incident's lifecycle boundary is proven fixed in the field, not patched plausibly — success by heroics is a fail._

## Acceptance Criteria

- [ ] A passing closing run is recorded for both legs with all positive criteria and all negative assertions holding, per the runbook template.
- [ ] The seeded 0.40-Zellij variant is recorded showing either recovery via Lisa's strings alone or the hazard unreachable by default (managed-runtime resolution with the old binary left unconsulted); any failure loops back as new tickets and this ticket stays open until a clean pass.
- [ ] The tour-probe rematch (same short prompt as the baseline probe, fresh session, fixed public surface) produces a page that names coding agents and the don't-babysit purpose unprompted — the comparison against the 2026-07-16 baseline tour is recorded.
