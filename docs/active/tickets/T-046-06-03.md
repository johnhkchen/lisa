---
id: T-046-06-03
story: S-046-06
title: closing-acceptance-run
type: task
status: open
priority: critical
agent: codex
phase: ready
depends_on: [T-046-06-02, T-046-01-02, T-046-02-02, T-046-03-02, T-046-04-02, T-046-07-01, T-046-07-02]
---

## Context

The epic's gate. With the floor enforcement, managed runtime, static artifacts, and string fixes landed, John manually reruns the protocol: a low-end agent handed the README install section must produce a doctor-green Lisa hands-off. Pass requires the primary matrix (both legs) to hit every positive criterion — `lisa doctor` exit 0, `lisa loop --dry-run` clean in a scaffolded project, ≤10 minutes, ≤~200MB disk — **and** every negative assertion: no rustc/cargo/rustup present afterward, no compiler invoked, sudo/apt actions recorded and boring. The seeded-failure variant (Zellij 0.40.1 on PATH) must show the agent recovering using only Lisa's own error strings. Human-operated and metered, same evidence discipline as the baseline ticket: absent run evidence means a named blocked state, not an assumed pass.

_Advances: the incident's lifecycle boundary is proven fixed in the field, not patched plausibly — success by heroics is a fail._

## Acceptance Criteria

- [ ] A passing closing run is recorded for both legs with all positive criteria and all negative assertions holding, per the runbook template.
- [ ] The seeded 0.40-Zellij variant is recorded showing recovery via Lisa's strings alone; any failure loops back as new tickets and this ticket stays open until a clean pass.
- [ ] The tour-probe rematch (same short prompt as the baseline probe, fresh session, fixed public surface) produces a page that names coding agents and the don't-babysit purpose unprompted — the comparison against the 2026-07-16 baseline tour is recorded.
