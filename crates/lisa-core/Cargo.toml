[package]
name = "lisa-core"
version = "0.1.0"
edition = "2021"
description = "Core types, ticket parsing, and DAG computation for Lisa"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml_ng = "0.9"
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
